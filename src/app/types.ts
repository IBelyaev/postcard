export type TextAlign = "center" | "left" | "right" | "justify";

export type TextStyle = {
    textAlign: TextAlign;
    color: string;
    fontWeight: string;
    fontSize: string;
    lineHeight: string;
    letterSpacing: string;
    fontFamily: string;
};
